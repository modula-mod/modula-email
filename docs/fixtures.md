# Fixtures

The `fixtures/` directory provides sample mailbox data for previews, docs, and validation flows.

## Included fixtures

- `sample-mailbox.json`
- `sample-thread.json`
- `sample-compose.json`

These fixtures are intentionally simple. They exist to make package review and runtime verification more repeatable.
